<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.mapper.ChatParticipantMapper">
    <!-- 참여자 추가 -->
    <insert id="insertParticipant" parameterType="com.example.demo.domain.ChatParticipant"
            useGeneratedKeys="true" keyProperty="participantId">
        INSERT INTO CHAT_PARTICIPANT
        (room_type, room_id, user_id, last_read_message_id, joined_at)
        VALUES
        ('GROUP', #{roomId}, #{userId}, 0, NOW())
    </insert>

    <!-- 방 인원수 확인 -->
    <select id="countParticipantsInRoom" resultType="int">
        SELECT COUNT(*)
        FROM CHAT_PARTICIPANT
        WHERE room_type = 'GROUP'
          AND room_id = #{gcrId}
    </select>

    <!-- 중복 참여 체크 -->
    <select id="existsUserInRoom" resultType="int">
        SELECT COUNT(*)
        FROM CHAT_PARTICIPANT
        WHERE room_type = 'GROUP'
          AND room_id = #{gcrId}
          AND user_id = #{userId}
    </select>

    <!-- 참여자 목록 조회 -->
    <select id="getParticipants" resultType="com.example.demo.domain.ChatParticipant">
        SELECT *
        FROM CHAT_PARTICIPANT
        WHERE room_type = 'GROUP'
          AND room_id = #{gcrId}
    </select>
    
   <!-- 방 나가기 -->
    <delete id="leaveRoom">
	    DELETE FROM CHAT_PARTICIPANT
	    WHERE room_type = 'GROUP'
	      AND room_id = #{gcrId}
	      AND user_id = #{userId}
	</delete>
    

</mapper>
